/* type.h's implementation, kept out of main.c for the same reason as pdf.c:
   CoreFoundation's MacTypes.h and screen.h both define a type called Style. */
#define TYPE_IMPLEMENTATION
#include "type.h"
